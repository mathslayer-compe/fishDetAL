## Config (config)
Contains examples of config files for each of the cli's.

yolo_dataset_config is an example format for yolo_data_setup_cli.

training_config_yolo is an example format of the required config file for yolo_trainer_cli.
